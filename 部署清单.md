# 上线检查清单

详细操作见 `DEPLOY.md`，Nginx 规则见 `nginx.example.conf`。

## 上传前

- [ ] PHP 版本至少为 8.1
- [ ] 已启用 `pdo_mysql`、`mbstring`、`openssl`
- [ ] 已创建 `townsite` MySQL 数据库和专用账号
- [ ] 已导入 `api/schema.sql`
- [ ] 仓库中不存在 `api/config.php`
- [ ] 仓库中不存在 `api/config.dev.php`
- [ ] 仓库中不存在 `api/selftest.php`

## 服务器配置

- [ ] 已从 `api/config.example.php` 创建 `api/config.php`
- [ ] 已填写站点数据库配置
- [ ] AuthMe 使用只读账号，或暂时设置为禁用
- [ ] 已填写 `site_owner`
- [ ] 已设置正确的域名和 PHP-FPM socket
- [ ] Nginx 已禁止访问配置、SQL、迁移、`.git` 和内部库
- [ ] 已申请 HTTPS 证书
- [ ] `security.require_https` 为 `true`

## 发布

Git 部署：

```bash
bash deploy.sh
```

ZIP/SFTP 部署：

```bash
bash deploy.sh --no-pull
```

## 上线后

- [ ] 首页、Wiki、资料页和工单页正常打开
- [ ] `/api/me.php` 返回 JSON
- [ ] `/api/config.php` 返回 403/404
- [ ] 普通玩家可以登录和提交工单
- [ ] 站长可以进入管理后台并回复工单
- [ ] Nginx 与 PHP-FPM 日志没有新增错误
- [ ] 已备份 `api/config.php` 和 `townsite` 数据库
