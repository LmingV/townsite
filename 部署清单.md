# 从空服务器到网站上线

服务器：东京节点，2 核 2G，公网 IP `154.36.158.143`，宝塔 Debian12/LNMP。

顺序照着来，每步都有验证方法。卡住了把报错发我。

---

## 第一步：先把宝塔面板护住

**这一步别跳过。** 宝塔默认端口 8888 全网都在扫，弱密码等于把服务器送人。MC 服官网被扫是常事。

登进宝塔后：

改面板端口（面板设置 → 安全 → 端口），换成一个五位随机数，别用 8888。改完防火墙要放行新端口，安全组也要开。

面板密码用长的，别和别处重复。

phpMyAdmin 限制访问（软件商店 → phpMyAdmin → 设置），设成只允许你家 IP，或者干脆平时关掉、要用时再开。

面板 SSH 端口也建议从 22 改掉。

---

## 第二步：Gitee 建仓库

新建仓库，名字随意（比如 `yhlg-site`），**选私有**。

网站代码本身不算机密，但私有仓库能避免一类风险：万一哪天误提交了配置文件，公开仓库等于立刻泄露。私有的话还有补救时间。

建好后**不要**用 Gitee 的初始化选项（不要勾"使用 Readme 初始化"），要一个空仓库。

---

## 第三步：本地推代码

在 `C:\Users\Administrator\Desktop\永恒流光官网` 里打开终端（右键空白处 → 在终端中打开），依次执行：

```powershell
git init
git add -A
git status
```

**第三条命令的输出要仔细看。** 确认列表里**没有**这三个文件：

- `api/config.php`
- `api/config.dev.php`
- `api/selftest.php`

它们含数据库密码和测试账号，`.gitignore` 应该已经挡住了。如果出现在列表里，**先停下告诉我**，别提交 —— Git 的特性是文件一旦进了历史，之后再加 `.gitignore` 也删不掉那份记录，密码仍能从旧提交里翻出来。那种情况下唯一干净的做法是改密码。

确认没问题再继续：

```powershell
git commit -m "初始版本"
git remote add origin https://gitee.com/你的用户名/yhlg-site.git
git push -u origin master
```

会让你输 Gitee 账号密码。

---

## 第四步：服务器上准备环境

宝塔里建网站（网站 → 添加站点）：

- 域名先填 `154.36.158.143`（域名解析好了再换成 `YHLG.LOVE`）
- PHP 版本选 **8.2**
- 数据库选 **MySQL**，宝塔会自动生成库名和密码，**把密码复制下来**

建好后记下网站根目录，通常是 `/www/wwwroot/154.36.158.143`。

然后建 Wiki 用的库（数据库 → 添加数据库）：

- 数据库名 `townsite`
- 字符集 **utf8mb4**（别选 utf8，那个存不了 emoji 和部分生僻字）
- 密码复制下来

---

## 第五步：拉代码

宝塔终端里执行（把路径和仓库地址换成你的）：

```bash
cd /www/wwwroot/154.36.158.143
rm -rf ./*                 # 清掉宝塔生成的默认首页
git clone https://gitee.com/你的用户名/yhlg-site.git .
```

最后那个点不能漏，意思是克隆到当前目录而不是新建子目录。

---

## 第六步：填配置

```bash
cd /www/wwwroot/154.36.158.143/api
cp config.example.php config.php
nano config.php
```

要填三处：

`site.pass` 填第四步 `townsite` 库的密码。

`authme` 那一段看情况。你的 MC 服托管在 `starmc.cn`，AuthMe 库在他们那边。要问服务商两件事：能不能远程连数据库、能不能给一个只有 SELECT 权限的账号。能就填他们给的地址和账号，并让他们放行 `154.36.158.143`。**不能的话把 `authme.enabled` 改成 `false`** —— 登录功能会停掉，但网站其余部分照常，Wiki 和游玩手册都能看。

`require_https` 先保持 `true`。如果暂时用 IP 访问、还没证书，临时改 `false`，**配好证书后一定改回来** —— `false` 意味着登录 cookie 能在明文 HTTP 上传输，会被中间人截获。

nano 保存是 Ctrl+O 回车，退出 Ctrl+X。

---

## 第七步：导入数据库

宝塔 → 数据库 → 找到 `townsite` → 管理（打开 phpMyAdmin）→ 导入 → 选 `api/schema.sql` → 执行。

或者命令行：

```bash
cd /www/wwwroot/154.36.158.143
mysql -u townsite -p townsite < api/schema.sql
```

导完确认有这几张表：`wiki_pages`、`wiki_revisions`、`wiki_submissions`、`login_fails`。

---

## 第八步：跑部署脚本

```bash
cd /www/wwwroot/154.36.158.143
chmod +x deploy.sh
./deploy.sh
```

它会检查 PHP 版本、配置是否就位、密码有没有填、全部 PHP 文件语法、危险文件有没有上线，最后修好属主和权限（`config.php` 收紧成 600）。

**以后每次要上线新改动，就只需要这一条命令。** 我改完代码你 `git push`，然后服务器上 `./deploy.sh`。

---

## 第九步：验证（这步最重要）

**1. 配置文件不能被下载**

浏览器访问 `http://154.36.158.143/api/config.php`

必须是 403、404 或空白。**如果显示出 PHP 源码，数据库密码就泄露了**，立刻告诉我。

**2. 首页正常**

访问 `http://154.36.158.143/`，确认在线人数显示出来了（应该是真实数字，从 `motd.minebbs.com` 查的）。

**3. Wiki 和手册分开了**

`/wiki.html` 应该显示"还没有词条"，`/wiki.html?p=manual` 应该能看到那十几篇教程。

**4. 登录**

如果 `authme.enabled` 是 `true`，试着用你的游戏账号登录。失败的话把浏览器控制台（F12）的报错发我。

---

## 第十步：域名和 HTTPS

域名解析：在域名注册商那里把 `YHLG.LOVE` 的 A 记录指向 `154.36.158.143`。

**注意别动 MC 服的解析。** 你的 `YHLG.LOVE` 现在有 SRV 记录指向 `mc6.starmc.cn:16001`，那是玩家进游戏用的。加 A 记录不影响 SRV，两者共存。但如果注册商界面让你"删除现有记录"，要小心 —— 删掉 SRV 玩家就进不了服了。

解析生效后（`ping YHLG.LOVE` 返回 `154.36.158.143`），宝塔里给网站加上域名，然后 SSL → Let's Encrypt → 申请证书，勾上"强制 HTTPS"。

证书装好后，确认 `config.php` 里 `require_https` 是 `true`。

---

## 之后的日常

我改代码 → 你在本地 `git add -A && git commit -m "说明" && git push` → 服务器上 `./deploy.sh`。

想更省事可以配自动拉取（宝塔 → 计划任务 → Shell 脚本，每 5 分钟跑一次 `deploy.sh`），但我建议先手动跑一段时间，出问题好定位。

**改坏了怎么回退：**

```bash
cd /www/wwwroot/154.36.158.143
git log --oneline -5          # 看最近几个版本
git reset --hard 上一个版本号
./deploy.sh
```

`config.php` 不在版本库里，回退不会影响它。
